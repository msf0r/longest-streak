<div class="header">
    {% if app.user is not empty %}
        <span class="pull-right"><a href="#" >{{ app.user.login }}</a></span>
    {% else %}
        <a class="btn btn-success pull-right" href="{{ path('hwi_oauth_service_redirect',{'service': 'my_github'}) }}" role="button">Join battle</a>
    {% endif %}
    <h3 class="text-muted">Project name</h3>
</div>